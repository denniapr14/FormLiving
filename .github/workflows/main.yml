name: Deploy Laravel Project on push
on:
  push:
    branches:
      - main
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Get the latest code
        uses: actions/checkout@v3
      # - uses: actions/setup-node@master
      # - name: Create tag
      #   uses: actions/github-script@v3
      # - uses: actions/javascript-action@v1
      - name: Copy .env
        run: php -r "file_exists('.env') || copy('.env.example', '.env');"
      - name: Install Dependencies
        run: composer update --ignore-platform-reqs
      - name: Generate key
        run: php artisan key:generate
      - name: Generate storage link
        run: php artisan storage:link
      - name: Directory Permissions
        run: chmod -R 777 storage bootstrap/cache
      - name: Laravel Deploy
        # You may pin to the exact commit or the version.
        # uses: ngocquyhoang/laravel-deploy@44ec6bf06ce9f68f68f614033fb57a8851f60b53
        uses: ngocquyhoang/laravel-deploy@44ec6bf06ce9f68f68f614033fb57a8851f60b53
        with:
          # Deploy user
          user: ${{ secrets.FTP_USERNAME }}
          # password: ${{ secrets.FTP_PASSWORD }}
          # Deploy host
          host: ${{ secrets.FTP_SERVER }}
          # Path source on server
          path: /public_html/webtest.formsliving.com/
          # Deploy owner
          owner: denniapr14/FormLiving

      # - name: 📂 Sync files
      #   uses: denniapr14/FormLiving@v1.0
      #   with:
      #     server: ${{ secrets.FTP_SERVER }}
      #     username: ${{ secrets.FTP_USERNAME }}
      #     password: ${{ secrets.FTP_PASSWORD }}
      #     server-dir: /public_html/webtest.formsliving.com/
